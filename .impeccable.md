## Design Context

### Users

- 工程试验室的试验工程师在办公室或现场试验间维护配合比版本、校验温度序列质量并复核成熟度计算。
- 施工技术人员在浇筑和养护交接时快速确认区段状态、目标强度、最新温度覆盖和预计达标时间。
- 独立复核人与审计人员需要追溯公式版本、冻结输入、逐步计算和前后快照，不能只看一个预测数字。
- 核心任务是用真实温度观测和版本化标定数据辅助养护判断；系统不替代实体试块试验、工程签认，也不控制加热、喷淋、模板或现场设备。

### Brand Personality

精确、克制、可追溯。界面应像一套经过校准的材料试验工作台：安静但不空泛，严谨但不冷漠，关键异常能被迅速发现，任何预测结论都能展开查看依据。

### Aesthetic Direction

- 采用浅色“材料试验台账”方向，而不是通用 SaaS 仪表盘或营销落地页。
- 主基底使用偏冷矿物灰和纸白；模板绿承担主操作与可用状态，养护琥珀表示待复核/质量提醒，氧化红表示无效或阻断。避免单一蓝紫色、渐变、发光、玻璃拟态、圆球装饰和纯黑纯白。
- 布局以侧栏、工作表、分隔线、状态轨迹和真实成熟度曲线为主要视觉结构。卡片只用于可比较的独立记录，不嵌套卡片；圆角控制在 4px。
- 使用固定的产品 UI 字号与表格数字对齐，不按视口缩放字体。正文优先可靠的本地无衬线字体，数字启用 tabular-nums。
- 动效只用于抽屉、状态变化和加载反馈，使用 transform/opacity 与快速减速曲线；尊重 `prefers-reduced-motion`。

### Design Principles

1. 证据先于结论：预测分值、ETA 和置信度附近必须能看到温度覆盖、公式版本、输入哈希、插值步骤和外插标志。
2. 状态一眼可扫：状态同时使用文案、图标/形状和颜色，不依赖颜色单独传达；所有状态迁移显示当前版本和执行角色。
3. 操作贴近工作流：常用动作直接可见，危险或不可逆动作使用明确动词；加载、成功、错误和无权限状态都有具体反馈。
4. 紧凑但不拥挤：采用 4px 空间基线、清晰分组和不等距节奏；数据表允许快速比较，详情通过侧向计算抽屉渐进展开。
5. 移动端不删能力：导航改为可换行的紧凑工具栏，表格转为可读记录行或允许受控横向查看，按钮保持至少 44px 触达区域，不出现页面级横向溢出。
6. 安全边界持续可见：登录后每个工作区都保留简短边界提示，禁止使用暗示自动放行拆模、自动控制设备或取代持证判断的文案。

### Accessibility And Quality

- 正文和控件按 WCAG AA 对比度设计，焦点环清晰，表单使用可见标签，图标按钮有 `aria-label` 和 tooltip。
- 页面提供跳到主内容的链接；键盘可访问导航、表单、表格操作和抽屉。
- 桌面至少验证 1440x1000，移动端至少验证 390x844；两者均检查文字遮挡、页面横向溢出、控制台错误和阻断请求。
